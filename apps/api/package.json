{
  "name": "@forge-deals/api",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node --watch src/server.ts",
    "build": "echo \"TODO: add build\"",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo \"TODO: add eslint\""
  },
  "dependencies": {
    "@forge-deals/shared": "file:../../packages/shared",
    "bullmq": "^5.56.0",
    "fastify": "^5.3.2",
    "ioredis": "^5.6.1",
    "pino": "^9.6.0",
    "zod": "^3.25.0"
  }
}
